import { PersonasManager } from "@/components/personas-manager"

export const metadata = {
  title: "Agent Personas — VaaniOS",
}

export default function PersonasPage() {
  return <PersonasManager />
}
